# OS junk
.DS_Store
Thumbs.db
desktop.ini

# IDEs
.idea/
.vscode/
*.iml
*.ipr
*.iws
.settings/
.classpath
.project

# Java / Gradle
build/
.gradle/
out/
bin/
*.class
*.jar
!**/gradle/wrapper/gradle-wrapper.jar
hs_err_pid*
replay_pid*

# Forge / MC dev runs
run/
runs/
runClient/
runServer/
runData/
.minecraftForge/
src/generated/

# Node
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# env / local config
.env
.env.local
agent-link-local.toml
.claude/settings.local.json

# local one-off test scripts (not part of the project)
smoke_test.py

# Release staging — built artifacts assembled for a GitHub release. Files here
# are uploaded to the release; they don't live in the repo.
release-staging/

# Session handover — local-only working notes for the next Claude Code session.
# Not for the team; lives only on this machine.
HANDOVER.md
